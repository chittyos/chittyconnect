# Authority — Services Entries

Add JSON entries here for services to be validated by:
- JSON Schema: etc/authority/schema/service.schema.json
- Rules: etc/authority/rules.json (checked by scripts/check-authority-rules.js)

Fields include service_name, category, status, owners, github_repo, primary_domain, exposes, routes, ontology, scopes.

