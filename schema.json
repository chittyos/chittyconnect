{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "chittyconnect API Schema",
  "type": "object",
  "properties": {
    "service": { "type": "string", "enum": ["chittyconnect"] },
    "status": { "type": "string" },
    "timestamp": { "type": "string", "format": "date-time" }
  },
  "required": ["service", "status", "timestamp"]
}