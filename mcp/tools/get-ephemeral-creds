{
  name: "get_ephemeral_credentials",
  description: "Fetch short-lived GitHub + NPM credentials for CI/CD",
  input_schema: {
    type: "object",
    properties: {
      api_key: { type: "string" }
    },
    required: ["api_key"]
  }
}
